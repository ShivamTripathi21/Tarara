import {
    onGetNearbyShowsListAction
} from './actions';

const OnGetNearbyShowsList = dispatch => UserLocation =>{
    dispatch(onGetNearbyShowsListAction(UserLocation));
}

const operations = {
    OnGetNearbyShowsList
};

export default operations;

